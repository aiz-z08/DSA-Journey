class Solution:
    def countTriples(self, n: int) -> int:
        squares={i*i for i in range(1,n+1)}
        count=0
        for j in range(1,n+1):
            for k in range(1,n+1):
                if j*j+k*k in squares:
                    count+=1
        return count
